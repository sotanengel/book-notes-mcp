registry=https://npm.flatt.tech/
save-exact=true
# NOTE: --ignore-scripts is applied in CI workflows.
# Locally, native modules (better-sqlite3) require build scripts.
# Run: npm ci && npm rebuild better-sqlite3
